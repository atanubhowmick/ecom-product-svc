#Spring application name
spring.application.name=product-svc

# Server port
server.port=@server.port@

# Active Profile
spring.profiles.active=@boot.profile@

#Expose all the actuator endopint
management.endpoints.web.exposure.include=*

#provide the actuotor info
info.app.name=product-svc
info.app.description=This application provides rest service api(s) to search the product details.
info.app.version=1.0.0

# Logging Properties
logging.level.org.springframework=DEBUG
logging.level.com.atanu.spring=DEBUG
logging.file.clean-history-on-start=false
logging.file.name=/log/product-svc.log
logging.file.max-size=20MB

# Registering as eureka client in Eureka server
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# Register url to server - changes for docker
eureka.client.service-url.defaultZone=@eureka.defaultzone@
#eureka.client.service-url.defaultZone=http://172.17.0.1:8085/eureka/

#eureka.instance.hostname=localhost
eureka.instance.prefer-ip-address=true
eureka.instance.ip-address=@instance.ip@
eureka.instance.instance-id=${eureka.instance.ip-address}:${server.port}:${spring.application.name}:${spring.profiles.active}:${spring.application.instance_id:${random.value}}

# H2 database properties
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=


# JPA Properties 
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# Enabling H2 Console
spring.h2.console.enabled=true
 
# Custom H2 Console URL
spring.h2.console.path=/h2

#Hazelcast config
spring.hazelcast.config=classpath:cache/hazelcast-client.xml
